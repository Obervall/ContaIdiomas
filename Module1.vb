Imports System.Windows.Forms

Module Module1
    Public Sub Main()

        '' NO SE USA DE MOMENTO, SOLO SI EL FORM INICIAL NO ES EL PRINCIPAL Y SE CAMBIA EN PROPIEDADES DE LA APLICACION
        ''frmpreferencias.ShowDialog()


        '' Habilitamos los estilos visuales.
        'Application.EnableVisualStyles()

        '' Mostramos el formulario Opciones.
        'Dim dlg As DialogResult = frmpreferencias.ShowDialog()
        'If (dlg = DialogResult.OK) Then
        '    ' Mostramos el formulario principal
        '    Application.Run(frmpreferencias)
        'End If
        '' Llegado a éste punto, finalizará la aplicación sin más.
    End Sub
End Module
